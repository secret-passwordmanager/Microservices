/**
 * Description. Will generate and return a refresh token that will 
 * belong to the user
 * @param {number} userId The id for the user you are trying to create
 * @param {Boolean} trusted Whether or not it is desired to create
 * @return {string} Returns a new refreshToken, which will be a 64 bit
 * string encoded in base64 
 */

function create(userId, trusted) {

}

/**
 * Description. Checks if the refresh token given exists
 * in @refreshTokens for the specified user.
 * @param {number} userId The user's userId
 * @param {string} token A refreshToken  
 * @return {boolean} Returns true if the refresh token is found,
 * otherwise returns false
 */
function exists(userId, token) {

}
function remove() {

}